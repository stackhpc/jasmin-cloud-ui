# jasmin-cloud-ui

Web portal frontend for the [JASMIN Cloud API](https://github.com/cedadev/jasmin-cloud).

## Build instructions

    yarn install
    yarn run build

This will generate static artifacts in a directory called `./dist/` which can
be served via NGINX.
